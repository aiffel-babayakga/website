# 🌐 GeneSpeak Web Demo
단일세포 전사체 기반 **약물 탐색 / 반응 예측 모델**을 웹 인터페이스로 시각화한 데모 프로젝트입니다.

본 레포지터리는 연구 모델의 추론 결과를 **웹 환경에서 직관적으로 조회·비교·탐색**할 수 있도록 구성된  
**Backend(API) + Frontend(Web)** 구조로 구성되어 있습니다.
<br/>
<br/>

## ✨ What This Web App Does
- 특정 **세포주(Cell line)** 와 **약물(Drug)** 조합에 대한 **예측된 유전자 발현 변화(ΔExpression)** 조회
- 쿼리 ΔExpression signature를 기준으로 **후보 약물 Retrieval / Ranking 결과 시각화**
- 연구용 모델 출력을 **누구나 쉽게 이해 가능한 형태의 웹 UI로 변환**
<br/>
<br/>

## 🧩 Project Structure
```
website-main/
├── app/                        # Backend (API 서버)
│   ├── main.py                 # FastAPI 엔트리포인트
│   ├── models.py               # 요청/응답 데이터 모델
│   └── services.py             # 모델 추론 / 후처리 로직
│
├── babayakga-frontend/         # Frontend (Web UI)
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── src/                    # React 기반 UI 코드
│
├── data/                       # 예제 입력 / 출력 데이터
├── requirements.txt            # Backend 의존성
└── README.md
```
<br/>
<br/>

## 🧠 Backend (API Server)
- **Framework**: FastAPI
- **역할**
  - 클라이언트 요청 수신
  - 약물/세포주 입력 처리
  - 모델 추론 결과 로딩 또는 계산
  - JSON 형태로 결과 반환
  
### 주요 파일
- `app/main.py`  
  → API 라우팅 및 서버 실행
- `app/services.py`  
  → 모델 추론, similarity 계산, ranking 로직
- `app/models.py`  
  → Request / Response schema 정의
<br/>
<br/>

## 🎨 Frontend (Web Interface)
- **Framework**: React + Vite
- **역할**
  - 사용자 입력(UI)
  - API 호출
  - Retrieval / Prediction 결과 시각화
  
### 특징
- 연구 데모 목적의 경량 UI
- 모델 결과 비교 및 랭킹 확인에 초점
<br/>
<br/>

## ▶️ How to Run

### 1️⃣ Backend 실행
```
pip install -r requirements.txt
uvicorn app.main:app --reload
```
  
기본적으로 `http://localhost:8000` 에서 API 서버가 실행됩니다.
<br/>

### 2️⃣ Frontend 실행

```
cd babayakga-frontend
npm install
npm run dev
```
  
브라우저에서 안내된 로컬 주소로 접속하면 됩니다.
<br/>
<br/>

## 🧪 Notes
- 본 웹 서비스는 **연구 데모 및 시각화 목적**으로 제작되었습니다.
- 대규모 모델 가중치 또는 전체 데이터셋은 포함되어 있지 않으며, 예제 입력 또는 사전 계산된 결과를 기반으로 동작할 수 있습니다.
- 실제 배포 환경에서는 모델 서빙 방식, 데이터 로딩 전략, API Latency 등에 대한 추가 최적화가 필요합니다.
<br/>
<br/>

## 📌 Intended Use

- 연구 발표 / 데모
- 약물 탐색 파이프라인 결과 시각화
- 모델 추론 결과에 대한 정성적 분석
<br/>
<br/>
